import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.medicinereminder.app',
  appName: 'MedicineReminder',
  webDir: 'out'
};

export default config;
